def main():
    """ 
    - A NESTED LOOP is when one loop is put inside another loop.
    - Nested loops help create more complex repeating code.
    - A NESTED LOOP has an INNER LOOP and an OUTER LOOP.
    - The inner loop always has to run through all its sequences 
      before the outer loop code is run again.
    """
    
    # while game is not over play background music (Bass Drum 7 times then Snare)

    pass


if __name__ == "__main__":
    main()
